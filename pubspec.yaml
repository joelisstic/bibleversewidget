name: bible_verse_widget
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  get_it: ^7.6.4
  home_widget: ^0.9.0
  shared_preferences: ^2.3.2
  workmanager: ^0.9.0
  firebase_core: ^3.11.0
  cloud_firestore: ^5.6.3
  firebase_auth: ^5.5.0
  google_sign_in: ^6.2.2
  intl: ^0.20.2

dependency_overrides:
  workmanager: ^0.9.0
  home_widget: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/bible_verses.json
    - assets/images/jo.png
